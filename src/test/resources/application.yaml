spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/flat_rent?currentSchema=test
    username: postgres
    password: postgres
    driver: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true